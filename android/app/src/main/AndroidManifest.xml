<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:appCategory="productivity"
        android:description="@string/app_description"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.ptashka.dayplan.SHORT_NAME" android:value="@string/app_short_name" />
        <meta-data android:name="com.ptashka.dayplan.SLOGAN" android:value="@string/app_slogan" />
        <meta-data android:name="com.ptashka.dayplan.DEVELOPER" android:value="@string/app_developer" />
        <meta-data android:name="com.ptashka.dayplan.CATEGORY" android:value="@string/app_category" />
        <meta-data android:name="com.ptashka.dayplan.PRINCIPLE" android:value="@string/app_principle" />
        <meta-data android:name="com.ptashka.dayplan.STYLE" android:value="@string/app_style" />
        <meta-data android:name="com.ptashka.dayplan.FEATURES" android:value="@string/app_features" />
        <meta-data android:name="com.ptashka.dayplan.KEYWORDS" android:value="@string/app_keywords" />
        <meta-data android:name="com.ptashka.dayplan.CHARACTER" android:value="@string/app_character" />
        <meta-data android:name="com.ptashka.dayplan.BRAND_ASSOCIATION" android:value="@string/app_brand_association" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Напоминания должны срабатывать минута в минуту.
         С API 33 хватает USE_EXACT_ALARM (выдаётся автоматически
         приложениям-календарям), на 31-32 нужен SCHEDULE_EXACT_ALARM. -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
